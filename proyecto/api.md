# API — bances-web

No aplica — sitio estático (Astro SSG) sin API propia.

Única excepción futura: el endpoint PHP del formulario de citas (`POST`, PHPMailer — patrón logopedajessica-web/smedialab-web), que se implementará en la fase migración del workflow init-web-astro. Cuando exista, documentar aquí: ruta, campos (nombre, contacto, tratamiento, RGPD), validación y respuestas.
